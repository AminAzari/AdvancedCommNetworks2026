# مرور پیش‌نیازها — شبکه‌های ارتباطی پیشرفته

این سند مفاهیم بنیادی را که به‌عنوان دانش قبلی فرض می‌شوند مرور می‌کند. از آن به‌عنوان یادآوری پیش از ورود به ماژول‌های درس استفاده کنید.

---

## 1. مرور مدل OSI (با تمرکز بر مخابرات)

مدل 7-لایه OSI چارچوب مرجع را فراهم می‌کند. در شبکه‌های موبایل/مخابرات، مرتبط‌ترین لایه‌ها عبارتند از:

| لایه | نام | ارتباط با مخابرات |
|-------|------|-------------------|
| 1 | **فیزیکی** | انتقال RF، مدولاسیون، طراحی آنتن، کدگذاری کانال |
| 2 | **پیوند داده** | زمان‌بندی MAC، ARQ/HARQ، مدیریت منابع رادیویی |
| 3 | **شبکه** | مسیریابی IP، تونل‌سازی GTP، مدیریت تحرک |
| 4 | **انتقال** | TCP/UDP برای تحویل سرتاسری، SCTP در سیگنالینگ |
| 7 | **کاربرد** | IMS/SIP برای صدا، HTTP/2 در واسط‌های SBA در 5G |

**نکات کلیدی برای به خاطر سپردن:**
- لایه‌های 1–2 جایی هستند که بیشتر نوآوری‌های مختص بی‌سیم رخ می‌دهد (PHY/MAC).
- «صفحه کاربر» داده را حمل می‌کند (لایه 3+)، در حالی که «صفحه کنترل» سیگنالینگ را مدیریت می‌کند.
- در 5G، پشته پروتکل تقسیم می‌شود: CU (PDCP و بالاتر) در برابر DU (RLC/MAC) در برابر RU (PHY).
- تونل‌های GTP-U بسته‌های IP کاربر را بین ایستگاه پایه و هسته کپسوله می‌کنند.

---

## 2. مفاهیم پایه RF

### فرکانس و پهنای باند
- **فرکانس (f):** تعداد نوسان‌ها در ثانیه (Hz). باندهای موبایل از 700 MHz تا 100 GHz گسترده‌اند.
- **طول‌موج:** λ = c / f. فرکانس بالاتر ← طول‌موج کوتاه‌تر ← آنتن‌های کوچک‌تر اما تلفات مسیر بیشتر.
- **پهنای باند (BW):** محدوده فرکانس‌هایی که یک سیگنال اشغال می‌کند. پهنای باند بیشتر ← نرخ داده بالاتر (شانون).

### مدولاسیون
- **هدف:** نگاشت بیت‌های دیجیتال روی سیگنال‌های حامل آنالوگ.
- **طرح‌های معمول:** BPSK، QPSK، 16-QAM، 64-QAM، 256-QAM.
- مدولاسیون مرتبه بالاتر = بیت بیشتر در هر نماد اما نیازمند SNR بهتر.

### نسبت سیگنال به نویز (SNR)
- SNR = P_signal / P_noise (خطی) یا 10·log₁₀(P_signal/P_noise) بر حسب dB.
- ظرفیت شانون: C = BW · log₂(1 + SNR) [بیت/ثانیه].
- تعیین می‌کند کدام طرح مدولاسیون و کدگذاری (MCS) می‌تواند استفاده شود.

### مرجع سریع
```
QPSK  → 2 bits/symbol → robust, used at cell edge
64-QAM → 6 bits/symbol → high throughput, needs high SNR
256-QAM → 8 bits/symbol → peak rates, very close to base station
```

---

## 3. مبانی ارتباطات دیجیتال

### نرخ خطای بیت (BER)
- احتمال اینکه یک بیت دریافتی با بیت ارسالی متفاوت باشد.
- به SNR، مرتبه مدولاسیون و کدگذاری بستگی دارد.
- اهداف معمول: 10⁻³ (صدا)، 10⁻⁶ (داده پیش از ARQ).

### کدگذاری کانال
- **هدف:** افزودن افزونگی برای تشخیص/تصحیح خطا.
- **کدهای توربو:** در 3G/4G برای کانال‌های داده استفاده می‌شوند.
- **کدهای LDPC:** در 5G NR برای داده (eMBB) استفاده می‌شوند.
- **کدهای پولار:** در 5G NR برای کانال‌های کنترل استفاده می‌شوند.
- **نرخ کد (R):** نسبت بیت‌های اطلاعات به کل بیت‌ها. R = k/n (مثلاً R = 1/2 یعنی 50% افزونگی).

### مفهوم OFDM
- **مسئله:** کانال‌های پهن‌باند از محوشدگی انتخابی-فرکانسی رنج می‌برند.
- **راه‌حل:** تقسیم کانال پهن‌باند به بسیاری زیرحامل باریک، که هر یک محوشدگی تخت را تجربه می‌کند.
- **پارامترهای کلیدی:**
  - فاصله زیرحامل (Δf): 15 kHz (LTE)، 15/30/60/120/240 kHz (5G NR).
  - اندازه FFT: تعداد زیرحامل‌ها را تعیین می‌کند.
  - پیشوند چرخه‌ای (CP): بازه محافظ برای مقابله با تداخل بین-نمادی (ISI).
- **مزایا:** یکسان‌سازی ساده (1-ضربه در هر زیرحامل)، تخصیص منبع انعطاف‌پذیر.
- **OFDMA:** چندین کاربر زیرحامل‌ها را به اشتراک می‌گذارند ← پایه پیوند پایین‌سو LTE و 5G.

---

## 4. مبانی شبکه‌سازی IP

### آدرس‌دهی
- **IPv4:** آدرس‌های 32 بیتی (مثلاً 192.168.1.1)، NAT در شبکه‌های موبایل رایج است.
- **IPv6:** آدرس‌های 128 بیتی، به‌طور فزاینده در 5G استفاده می‌شود (از NAT اجتناب می‌کند).
- **زیرشبکه‌بندی:** نشانه‌گذاری CIDR (مثلاً 10.0.0.0/24 = 256 آدرس).

### مسیریابی
- شبکه‌های هسته موبایل از مسیریابی IP بین توابع شبکه استفاده می‌کنند.
- **مسیرهای ایستا** برای توپولوژی‌های ساده؛ **BGP/OSPF** در شبکه‌های حامل.
- **تونل‌سازی GTP:** بسته‌های IP مشترک را درون GTP/UDP/IP بین gNB و UPF کپسوله می‌کند.

### پروتکل‌های انتقال
| پروتکل | ویژگی‌ها | کاربرد در مخابرات |
|----------|-----------|----------------|
| **TCP** | قابل اعتماد، مرتب، کنترل ازدحام | HTTP/2 (SBA در 5G)، ترافیک وب |
| **UDP** | غیرقابل اعتماد، کم-تأخیر | VoIP/RTP، صفحه کاربر GTP-U |
| **SCTP** | چند-میزبانی، پیام-محور | سیگنالینگ S1AP/NGAP (4G/5G) |

### DNS و NAT
- UEها آدرس IP را از طریق برقراری نشست PDN/PDU به‌دست می‌آورند (نه مستقیماً DHCP).
- حل DNS برای کشف خدمات در SBA در 5G (NRF).

---

## 5. احتمال پایه

### چرا احتمال در مخابرات؟
- دسترسی تصادفی (RACH): احتمال برخورد با چندین کاربر.
- زمان‌بندی: مدل‌سازی رسیدن ترافیک (فرآیند پواسون).
- محوشدگی: توزیع‌های رایلی/رایسی چندمسیری را مدل می‌کنند.
- تحلیل BER: تابع Q، محاسبات احتمال خطا.

### مفاهیم کلیدی
- **متغیر تصادفی (X):** نتیجه یک آزمایش تصادفی.
- **PDF / PMF:** تابع چگالی/جرم احتمال.
- **امید ریاضی:** E[X] = Σ xᵢ · P(xᵢ) — نتیجه میانگین.
- **واریانس:** Var(X) = E[(X - μ)²] — پراکندگی حول میانگین.

### توزیع‌هایی که با آن‌ها روبه‌رو خواهید شد
| توزیع | مورد استفاده |
|-------------|----------|
| **یکنواخت** | تایمرهای عقب‌نشینی تصادفی |
| **نمایی** | زمان‌های بین-ورود (بدون حافظه) |
| **پواسون** | تعداد ورودها در یک پنجره زمانی |
| **رایلی** | پوش چندمسیری محوشدگی (NLOS) |
| **گاوسی (نرمال)** | مدل‌سازی نویز (AWGN) |

### فرمول مفید
- احتمال برخورد در ALOHA شکاف‌دار با n کاربر، 1 شکاف: P(collision) = 1 - (1 - 1/K)^n ≈ 1 - e^(-n/K) برای K بزرگ.

---

## 6. ریاضیات دسی‌بل

### چرا دسی‌بل؟
سیگنال‌های بی‌سیم گستره دینامیکی عظیمی را در بر می‌گیرند (pW تا W). دسی‌بل این را به اعداد قابل مدیریت فشرده می‌کند و ضرب‌ها را به جمع تبدیل می‌کند.

### تعاریف

| واحد | فرمول | مرجع |
|------|---------|-----------|
| **dB** | 10·log₁₀(P₁/P₂) | نسبت (بی‌بعد) |
| **dBm** | 10·log₁₀(P / 1 mW) | توان مطلق نسبت به 1 mW |
| **dBW** | 10·log₁₀(P / 1 W) | توان مطلق نسبت به 1 W |

### تبدیل‌های معمول

| خطی | dB |
|--------|-----|
| ×2 | +3 dB |
| ×10 | +10 dB |
| ×100 | +20 dB |
| ×1000 | +30 dB |
| ×0.5 | −3 dB |
| ×0.1 | −10 dB |

### جدول مرجع سریع

| توان | dBm |
|-------|------|
| 1 mW | 0 dBm |
| 10 mW | 10 dBm |
| 100 mW | 20 dBm |
| 1 W | 30 dBm |
| 20 W (ایستگاه پایه ماکرو) | 43 dBm |

### بودجه پیوند (پیش‌نمایش)
```
Received Power (dBm) = Tx Power (dBm) + Tx Gain (dBi) - Path Loss (dB) + Rx Gain (dBi)
```

### مسائل تمرینی
1. یک فرستنده 2 W خروجی می‌دهد. بر حسب dBm بیان کنید. ← 10·log₁₀(2000) = 33 dBm.
2. تلفات مسیر 130 dB است، Tx = 43 dBm، بهره آنتن‌ها مجموعاً 18 dBi. توان Rx؟ ← 43 + 18 − 130 = −69 dBm.
3. SNR = 20 dB. نسبت خطی چیست؟ ← 10^(20/10) = 100.

---

## چک‌لیست خلاصه

پیش از شروع ماژول 2، تأیید کنید که می‌توانید:

- [ ] شناسایی کنید کدام لایه‌های OSI توابع PHY، MAC و IP را انجام می‌دهند
- [ ] بین توان خطی و dBm تبدیل کنید
- [ ] توضیح دهید چرا OFDM با محوشدگی انتخابی-فرکانسی مقابله می‌کند
- [ ] ظرفیت شانون را با داشتن BW و SNR محاسبه کنید
- [ ] تفاوت بین TCP و UDP و محل استفاده هر یک را توصیف کنید
- [ ] احتمالات ساده برخورد را برای دسترسی تصادفی محاسبه کنید

اگر هر موضوعی ناآشنا به نظر می‌رسد، بخش‌های ارجاع‌شده را مرور کنید یا به کتاب‌های درسی توصیه‌شده در طرح درس مراجعه کنید.

---

*آخرین به‌روزرسانی: اوت 2026*
